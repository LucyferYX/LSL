# LSL
Experimental model and dataset repository for Latvian Sign Language recreation project.

## Setup

Install dependencies:

pip install -r requirements.txt

Download base model `mt5-small` from huggingface.